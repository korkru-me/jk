# ประเภทโจทย์ `image_label` (ติดป้ายบนรูป)

อัปเดตล่าสุด: 17 กันยายน 2026 · สถานะ: **เฟส 0–2 เสร็จ · migration apply บนฐานจริงแล้ว · ยังไม่มี UI**
· เบรนช์ `feat/image-label-question-type` แตกจาก `master` ที่ `fe687a3`

เอกสารนี้คือช่องทาง handoff ของงานนี้ตาม `AGENTS.md` แชตและ memory ของ agent ไม่ใช่ช่องทาง handoff
ใครมาทำต่อ (Claude Code หรือ Codex เครื่องไหนก็ตาม) ให้เริ่มจากไฟล์นี้

## โจทย์ที่ต้องการรองรับ

ใบงาน "ระบุส่วนประกอบจากภาพ" ที่ครูวิทย์/ชีววิทยาใช้กันทั่วไป — ครูแนบรูปไดอะแกรมมาหนึ่งใบ
แล้ววางกล่องคำตอบชี้ไปที่จุดไหนของรูปก็ได้ ตัวอย่างต้นแบบคือใบงาน "อวัยวะในระบบหายใจ":
รูปทางเดินหายใจหนึ่งใบ · กล่องคำตอบ 7 กล่องกระจายรอบรูป แต่ละกล่องมีเส้นโยงไปยังอวัยวะของมัน ·
ใต้รูปมีคลังคำ 9 คำ (จมูก หลอดลม ปอด โพรงจมูก หลอดลมฝอย กระบังลม ท่อลม ถุงลม กระดูกซี่โครง)
ให้ลากมาใส่กล่อง

**7 กล่อง แต่ 9 คำ** — ใบงานจริงมีตัวลวง คลังคำยาวกว่าจำนวนจุดได้และต้องรองรับตั้งแต่รุ่นแรก

ครูต้องเลือกวิธีตอบได้เอง สามแบบ: พิมพ์เอง · ดรอปดาวน์ · ลากคำจากคลังมาใส่กล่อง

### ทำไมไม่ใช้ประเภทที่มีอยู่

ไม่มีชนิดไหนเก็บ**พิกัด**ได้เลย ซึ่งเป็นสิ่งเดียวที่โจทย์แบบนี้ต้องการจริง ๆ:

| ชนิดเดิม | ทำได้แค่ไหน | ตกตรงไหน |
|---|---|---|
| `fill_blank` | ช่องกรอก/ดรอปดาวน์ครบ ตรวจให้แล้ว | ช่องถูกวางด้วย marker `[___1]` **ในข้อความ** (`lib/fill-blank.ts`) ไม่มีที่ให้ x,y — เติม coordinate ลง `FillBlankConfig` คือเปลี่ยนความหมายของโจทย์เติมคำเก่าทุกข้อ |
| `matching` | มีทั้งโหมดลากและโหมดลากเส้นอยู่แล้ว | คู่เก็บใน `mcq_options` เป็นลิสต์สองคอลัมน์ ไม่มีพิกัด · เฉลย `MATCH:` คือ "ข้อความฝั่งขวาเรียงตามลำดับฝั่งซ้าย" ซึ่งผูกกับเลย์เอาต์สองคอลัมน์โดยตรง |
| `composite` | รวมหลายชนิดใต้โจทย์เดียว | `CompositePart` เรียงแนวตั้งเป็น ก) ข) ค) ไม่มีมิติเชิงพื้นที่เลย |

เหตุผลเดียวกับที่ `classify` ไม่ยอมใช้ `composite` (ดู `docs/CLASSIFY_QUESTION_TYPE.md`):
**ความสัมพันธ์ที่โจทย์ต้องการแสดง ชนิดเดิมแสดงไม่ได้** ในที่นี้คือ "กล่องนี้ชี้ไปที่จุดนี้ของรูป"

## สิ่งที่ยืมของเดิมได้ — และนี่คือเหตุผลที่งานนี้ไม่ใหญ่เท่าที่ชื่อฟัง

| ยืมอะไร | จากไหน | ยืมได้แค่ไหน |
|---|---|---|
| กติกาตรวจคำตอบข้อความ | `isBlankCorrect()` ใน `lib/fill-blank.ts` | ใช้ได้ทั้งดุ้น ไม่ต้องแก้ — `dropdown` เทียบเป๊ะ `fixed` เคารพ case_sensitive |
| รูปร่างเฉลยรายช่อง | `FillBlankItem` (`answers[]`, `case_sensitive`, `options[]`) | ลอกโครงมาเป็น marker ได้ตรง ๆ |
| เอนจินลาก | `components/exam/matching-drag-input.tsx` | pointer events ไม่ใช่ HTML5 DnD (touch ใช้ไม่ได้) · มี tap-to-place เป็นทางสำรอง · **`place()` รองรับ bank ยาวกว่าช่องอยู่แล้ว** ชิปที่ถูกเบียดออกกลับลงคลังเอง = ตัวลวงใช้ได้ |
| เส้นโยง | `components/exam/matching-line-input.tsx` | SVG overlay + ResizeObserver พร้อม · **ที่นี่ง่ายกว่าเดิม** เพราะปลายเส้นข้างหนึ่งคำนวณจาก % บนรูป ไม่ต้องวัด DOM |
| อัปโหลดรูป | `QuestionImageUpload` + `lib/image-downscale.ts` | ใช้ซ้ำได้ ย่อรูปให้อัตโนมัติอยู่แล้ว |
| กวาดไฟล์กำพร้า | `lib/storage-release.ts` | **ได้ฟรี** — มันอ่าน `JSON.stringify(row)` ทั้งแถว จึงเห็น URL ที่อยู่ใน `extra_data` |
| content fingerprint | `lib/question-content-match.ts` | **ได้ฟรี** — `canonical(q.extra_data)` เป็น generic |

**ที่ต้องเขียนใหม่จริง ๆ มีแค่: รูป + พิกัด + เส้นโยง + เลย์เอาต์มือถือ + ฟอร์มวางจุด**

## การตัดสินใจที่อนุมัติแล้ว (17 กันยายน 2026)

| เรื่อง | ที่เลือก | ย้อนกลับได้ไหม |
|---|---|---|
| ชื่อ type | `image_label` | **ไม่ได้** — Postgres ลบค่า enum ไม่ได้ |
| prefix เฉลย | `IMGL:` | **ไม่ได้** — ตรึงลง `submission_answers.correct_answer` |
| ป้ายไทย | `TYPE_LABEL` = "ติดป้ายบนรูป" · `TYPE_SHORT` = "ป้ายบนรูป" | ได้ เป็นแค่ label |
| คะแนน | **จุดละ 1 คะแนน ได้คะแนนบางส่วน** — กติกาเดียวกับ `CLS:`/`COMP:`/`FILL:` | ได้ |
| ครูวางอะไร | **รุ่นแรกวาง "จุด" อย่างเดียว กล่องจัดอัตโนมัติริมซ้าย/ขวาเรียงตาม y** | ได้ — `box?` เป็น optional อยู่แล้ว เพิ่มการลากกล่องทีหลังไม่ทำลายโจทย์เก่า |
| โหมดตอบ | **ต่อข้อ ไม่ใช่ต่อจุด** — `typed` / `dropdown` / `drag` | ได้ |
| สลับลำดับคลังคำ | **ไม่สลับ** | ได้ |
| โหมดครูตรวจเอง | **ไม่มีในรุ่นแรก — ตรวจอัตโนมัติล้วน** `is_correct` ไม่เคยเป็น null | ได้ |
| หน่วยข้อย่อย | `subQuestionUnit` = "จุด" | ได้ |

**ที่ยังควรให้เจ้าของผลิตภัณฑ์ยืนยันอีกครั้งก่อนเฟส 1**: ป้ายไทย "ติดป้ายบนรูป"
เป็นคำที่ agent เลือกเอง ไม่ได้มาจากคำที่ครูใช้จริง — ถ้ามีคำที่ครูเรียกกันอยู่แล้วให้ใช้คำนั้น

**เหตุผลของสองข้อที่ไม่ชัดในตัวเอง:**

- **ไม่สลับคลังคำ** — `option_order` ในตาราง attempt ผูกกับ index ของ `mcq_options`
  (ดู `buildAttemptQuestion` ใน `lib/assignment-attempt.ts`) เอามาใช้ซ้ำกับคลังคำที่อยู่ใน `extra_data`
  คือ overload ความหมายของคอลัมน์ · และคลังคำในใบงานกระดาษเป็นกริดตายตัวอยู่แล้ว
  ถ้าจะสลับทีหลังต้องคิดเรื่องคอลัมน์ที่เก็บลำดับใหม่ ไม่ใช่ยัดลงของเดิม
- **ไม่มีโหมดครูตรวจเอง** — เหมือน `classify` ที่ตัดสถานะ "รอครูตรวจ" ทิ้งทั้งสาย
  ทำให้ไม่ต้องคิดเคส `is_correct = null` ในหน้าตรวจ หน้าผลสอบ และ `answer-feedback` เลย

## รูปร่างข้อมูล

`lib/types.ts` — `ImageLabelConfig` เก็บใน `questions.extra_data`

```ts
export type ImageLabelAnswerMode = 'typed' | 'dropdown' | 'drag'

export interface ImageLabelMarker {
  id: string
  /** ชื่อสั้นของจุด ใช้เรียกจุดนี้บนหน้าตรวจและหน้าผลสอบ — ไม่ใช่เฉลย และนักเรียนไม่เห็น */
  label?: string
  /** % ของความกว้าง/สูงรูป (0–100) ไม่ใช่ px */
  point: { x: number; y: number }
  /** undefined = จัดอัตโนมัติริมซ้าย/ขวา */
  box?: { x: number; y: number }
  /** เฉลยที่ยอมรับ — โครงเดียวกับ FillBlankItem.answers */
  answers: string[]
  case_sensitive: boolean
  /** เฉพาะโหมด dropdown; ไม่ใส่ = ใช้ bank ของข้อ */
  options?: string[]
}

export interface ImageLabelConfig {
  image_url: string
  answer_mode: ImageLabelAnswerMode
  /** เฉพาะโหมด drag — ยาวกว่าจำนวนจุดได้ ส่วนที่เกินคือตัวลวง */
  bank?: string[]
  markers: ImageLabelMarker[]
}
```

### สามจุดที่ตั้งใจเลือกแบบนี้ ห้ามแก้โดยไม่อ่าน

1. **`image_url` แยกจาก `questions.image_urls`**
   `components/exam/exam-client.tsx` พิมพ์ `image_urls` ออกมาเหนือช่องตอบ**โดยไม่ดูชนิดโจทย์เลย**
   (ดูราวบรรทัด 1021) เก็บรูปไดอะแกรมไว้ตรงนั้นแล้วนักเรียนจะเห็นรูปซ้ำสองใบ
   ใบหนึ่งเป็นภาพประกอบ อีกใบเป็นพื้นที่ตอบ

2. **`point` เป็น % ไม่ใช่ px**
   รูปกว้าง 1600px บนจอ 375px คือคนละสเกล เก็บ px แล้วจุดจะหลุดออกนอกรูปทันทีบนมือถือ
   `lib/image-downscale.ts` ย่อรูปทุกใบเหลือด้านยาวสุด 1600px อยู่แล้ว แต่ขนาดที่**แสดง**ยังต่างกันทุกจอ

3. **สามโหมดส่งคำตอบเป็น "ข้อความของจุดนั้น" เหมือนกันหมด**
   `["จมูก", "", "ปอด", …]` เรียงตามลำดับ marker · ตรวจด้วยกติกาเดียว เฉลยตรึงชุดเดียว
   ครูสลับโหมดทีหลังได้โดยไม่ทำให้คำตอบที่นักเรียนส่งไปแล้วใช้ไม่ได้
   เจตนาเดียวกับ `MatchingAnswerMode` ที่ `slots`/`lines` เป็นแค่การนำเสนอ

### เฉลยที่ตรึงไว้

```
IMGL:[{"a":["จมูก"],"cs":false},{"a":["โพรงจมูก"],"cs":false}, …]
```

หนึ่งรายการต่อหนึ่งจุดที่ครูใส่เฉลย เรียงตามลำดับ marker

**เฉลยที่ตรึงต้องพอให้ตรวจได้เองทั้งหมด ห้ามให้สาขาตรวจไปอ่าน `extra_data` สด**
ต่างจากสาขา `FILL:` ที่อ่าน `getBlankType`/`case_sensitive`/`naturalMaxScore` จาก `extra_data` ปัจจุบัน —
นั่นแปลว่าครูแก้โจทย์หลังนักเรียนส่งแล้ว กติกาตรวจจะเปลี่ยนย้อนหลัง
ที่นี่ทำแบบ `COMP:` ซึ่งตรึง `{ type, correct, blankType, caseSensitive, score }` ลงไปทั้งชุด
และแบบ `CLS:` ที่**เอาตัวหารมาจากเฉลยที่ตรึง ไม่ใช่จาก config ปัจจุบัน**

## กติกาที่ต้องไม่แก้โดยไม่อ่านก่อน

ทั้งหมดอยู่ที่ `lib/image-label.ts` ที่เดียว เพราะ **เฉลยที่ตรึง · คะแนนเต็ม · จำนวนข้อย่อยบนการ์ด ต้องเป็นเลขเดียวกัน**

1. **จุดที่ครูวางแต่ไม่ได้ใส่เฉลย ไม่นับเป็นคะแนนเต็ม และไม่ถูกตรวจ**
   เคยพลาดมาแล้วกับข้อย่อยเติมคำของ `composite` — `naturalMaxScore` นับข้อย่อยที่ตัวเรนเดอร์ไม่มีช่องให้กรอก
   นักเรียนเสียคะแนนให้ช่องที่ครูไม่เคยให้มา (ดู `docs/FEATURE_STATUS.md`)
   `imageLabelMarkerCount` ต้องนับเฉพาะจุดที่ตอบได้และตรวจได้

2. **โหมด `dropdown` ที่จุดนั้นไม่มี option ตรงกับเฉลยเลย ถือเป็นจุดที่ไม่ได้ใส่เฉลย**
   เกิดจากการนำเข้าไฟล์ หรือครูแก้ตัวเลือกทีหลัง ปล่อยไว้จะกลายเป็นจุดที่ไม่มีใครตอบถูกได้
   ตรงกับกฎข้อ 2 ของ `classify` ที่เฉลยชี้เลยจำนวนตัวเลือกแล้วถือว่าไม่ได้ใส่เฉลย

3. **โหมด `drag` ที่เฉลยของจุดไม่มีอยู่ใน `bank` ถือเป็นจุดที่ไม่ได้ใส่เฉลย**
   เหตุผลเดียวกับข้อ 2 — ลากอะไรมาใส่ก็ไม่มีวันถูก

4. **ตัวหารตอนให้คะแนนมาจากเฉลยที่ตรึงไว้ ไม่ใช่ `extra_data` ปัจจุบัน**
   ครูลบจุดทิ้งหลังนักเรียนตอบไปแล้ว ตัวหารสดจะหดลงขณะที่ `earned` ยังมาจากชุดเดิมที่ใหญ่กว่า
   แล้ว `scaleScore` จะจ่ายเกินคะแนนที่โจทย์มี — ต้องมีเทสต์คุมเหมือนที่ `classify` มี

5. **`image_label` ไม่มีสถานะ "รอครูตรวจ"** — `is_correct` ไม่เคยเป็น null

6. **ห้ามสลับลำดับ marker** — คำตอบนักเรียนเป็นลิสต์เรียงตามลำดับ marker เทียบกับเฉลยที่ตรึงจาก config เดียวกัน
   สับลำดับเมื่อไหร่ทุกจุดเลื่อนหมด (ปรนัยสับได้เพราะ attempt เก็บ `option_order` ไว้แมปกลับ ที่นี่ไม่มี)

## แผน 9 เฟส และสถานะ

โครงมาจาก `classify` ที่ทำสำเร็จมาแล้ว 7 เฟส **แต่เพิ่มอีก 2** เพราะงานนี้มีสองอย่างที่ `classify` ไม่มี:
ฟอร์มวางพิกัดบนรูป (พื้นผิวใหม่ทั้งอัน) และเลย์เอาต์มือถือที่เป็น**คนละเลย์เอาต์** ไม่ใช่แค่ตารางไหลใหม่

| เฟส | งาน | ด่านที่ต้องผ่าน | สถานะ |
|---|---|---|---|
| 0 | ตัดสินใจ · ตรวจ `supabase migration list` · แตกเบรนช์ · เขียนเอกสารนี้ | local = remote ครบ | ✅ เสร็จ |
| 1 | types + คะแนน + เทสต์ (TypeScript ล้วน ไม่แตะ DB ไม่แตะ UI) | `tsc` ✓ `npm test` ✓ | ✅ เสร็จ |
| 2 | migration **2 ไฟล์แยกกัน** + เทสต์ PGlite | SQL ให้เลขเดียวกับ TS | ✅ เสร็จ · apply บนฐานจริงแล้ว |
| 3 | `lib/exam-safe.ts` — `SafeImageLabelConfig` ไม่มีเฉลย | **ทดลองทำให้เฉลยหลุดแล้วต้องเห็นเทสต์ fail** | ⬜ |
| 4 | ตัวเรนเดอร์นักเรียน **เดสก์ท็อป** × 3 โหมด | ตอบจริงบนหน้าสอบแล้วได้คะแนนตรง | ⬜ |
| 5 | **เลย์เอาต์มือถือ** | วัดจาก DOM จริง 375/768/812/1024/1280 | ⬜ |
| 6 | **ฟอร์มสร้าง/แก้** (ตัววางพิกัด) · การ์ดเลือกประเภท · ป้ายชื่อ · ทำสำเนา | วนครบรอบบนฐานจริง | ⬜ |
| 7 | ปลายทางหลังสอบ — `answer-feedback.ts` · หน้าครูตรวจ · หน้าผลสอบ | ไม่มีสตริง `IMGL:` โผล่บนหน้าไหน | ⬜ |
| 8 | เอกสาร + นำเข้า/ส่งออก `.korkru.json` **ทดสอบด้วยมือ** | `classify` ข้ามข้อนี้ อย่าข้ามซ้ำ | ⬜ |

### เฟส 1 — ต้องแตะไฟล์ไหนบ้าง

ลอกจากรายการจริงที่ `classify` แตะ (`grep -rln "'classify'" lib/ app/ components/`):

- `lib/image-label.ts` (ใหม่) — เจ้าของกติกาทั้งหมด
- `lib/image-label.test.ts` (ใหม่)
- `lib/types.ts` — `'image_label'` ใน `QuestionType` + สาม interface + ต่อ union ของ `extra_data`
- `lib/assignment-attempt.ts` — `naturalMaxScore` · เฉลยตรึง `IMGL:` · สาขาให้คะแนน
- `lib/question-parts.ts` — `subQuestionCount` + `subQuestionUnit` = "จุด"
- `lib/question-sort.ts` — **ต่อท้าย `QUESTION_TYPE_ORDER`** ให้ตรงลำดับ enum ใน Postgres
- เทสต์เดิมที่ต้องเพิ่มเคส: `assignment-attempt.test.ts`, `question-parts.test.ts`, `question-sort.test.ts`

**tsc จะบังคับให้เติมอีก 3 จุดก่อนกำหนด** เพราะเป็น `Record<QuestionType, …>` หรือ union ที่ต้องครบ:
`lib/question-duplicate.ts` (route map → `'image-label'`) · `lib/question-portable.ts` (union ของ `extra_data`) ·
`app/(app)/questions/new/_data/sample-questions.ts` — **ใส่ใบงานระบบหายใจจริงเป็น fixture ตรงนี้
แล้วเฟส 4–5 จะมีข้อมูลทดสอบใช้ก่อนที่ฟอร์มจะมีตัวตน**

## สิ่งที่เฟส 1 ทำไปแล้ว

- `lib/image-label.ts` (ใหม่) — รูปร่างเฉลย การนับจุด และกติกาเทียบคำตอบ ที่เดียว
- `lib/image-label.test.ts` (ใหม่) — 31 เทสต์
- `lib/types.ts` — `'image_label'` ใน `QuestionType` · `ImageLabelAnswerMode`/`ImageLabelMarker`/`ImageLabelConfig` · ต่อเข้า union ของ `extra_data`
- `lib/assignment-attempt.ts` — `naturalMaxScore` · เฉลยตรึง `IMGL:` · สาขาการให้คะแนน
- `lib/question-parts.ts` — นับ "จุด"
- `lib/question-sort.ts` — ต่อท้าย `QUESTION_TYPE_ORDER`
- เทสต์เดิมที่เพิ่มเคส: `assignment-attempt.test.ts` (13) · `question-parts.test.ts` (2) · `question-sort.test.ts` · `streak-completion.test.ts`

**tsc บังคับให้เติม 3 จุดก่อนกำหนด ตรงตามที่เฟส 0 คาดไว้**:
`lib/question-duplicate.ts` (route map → `'image-label'`) · `lib/question-portable.ts` (union) ·
`app/(app)/questions/new/_data/sample-questions.ts`
พร้อม `imageLabelConfig?` ใน `QuestionPreviewProps` เพื่อให้ตัวอย่างถือข้อมูลได้ — **ยังไม่มีตัวเรนเดอร์อ่านมัน**

### เฉลยที่ตรึงออกมาหน้าตาแบบนี้

```
IMGL:[{"answers":["เซลล์ไฟฟ้า"],"exact":true},{"answers":["สวิตช์"],"exact":true}]
```

`exact` คำนวณครั้งเดียวตอนตรึง จากโหมดของข้อและ `case_sensitive` ของจุดนั้น
**สาขาให้คะแนนไม่อ่าน `extra_data` เลยสักบรรทัด** — ทั้งเฉลย วิธีเทียบ และตัวหาร มาจากเฉลยที่ตรึงทั้งหมด
ต่างจากสาขา `FILL:` ที่อ่าน `case_sensitive` และ `naturalMaxScore` สด ๆ ทุกครั้งที่ตรวจ

### สองอย่างที่ตัดสินใจระหว่างทาง ต่างจากแผนเฟส 0

1. **fixture เป็นวงจรไฟฟ้า ไม่ใช่ใบงานระบบหายใจ** — ในเรโปไม่มีรูปไดอะแกรมระบบหายใจ
   และการวาดภาพกายวิภาคเองแล้ววาดผิดจะแย่กว่าไม่มีตัวอย่าง จึงวาด
   `public/samples/simple-circuit.svg` เป็นแผนผังวงจรอย่างง่าย (เซลล์ไฟฟ้า สวิตช์ หลอดไฟ แอมมิเตอร์ สายไฟ)
   ซึ่งวาดให้ถูกตามสัญลักษณ์มาตรฐานได้ และเข้ากับตัวอย่างฟิสิกส์อื่นในไฟล์เดียวกัน
   **คุณสมบัติที่เฟส 4–5 ต้องใช้ยังครบ**: 5 จุด · คลังคำ 7 คำ (มีตัวลวง 2) · โหมด `drag` · พิกัดเป็น %
   ตรวจด้วยตาแล้วว่า SVG เรนเดอร์ถูกต้อง

2. **เพิ่มไฟล์ asset ในเฟสที่บอกว่า "TypeScript ล้วน"** — ตั้งใจ
   `SAMPLE_QUESTIONS` เป็น `Record<QuestionType, …>` ปล่อยเป็นอาเรย์ว่างก็ผ่าน tsc
   แต่ `app/exam-screen-lab/_lib/fixture.ts` อ่าน `SAMPLE_QUESTIONS[source][0].props` ตรง ๆ
   อาเรย์ว่างคือกับดักที่รอเฟส 4 อยู่

### สิ่งที่ตรวจแล้วว่าไม่ต้องแก้

`isInstantCheckable` และ `isStreakEligible` ใน `lib/grading.ts` เป็น **denylist** (ตัด `essay` กับ `file_upload` ออก)
`image_label` จึงกดตรวจทีละข้อได้และเข้าพูล "ถูกติดต่อกัน" ได้เองโดยไม่ต้องแก้อะไร
ซึ่งถูกต้องสำหรับประเภทที่ตรวจอัตโนมัติล้วนและไม่มีสถานะรอครูตรวจ — ปักหมุดไว้ด้วยเทสต์แล้ว

### ตรวจแล้ว

`tsc` ✓ · `npm test` 1092 ✓ (เพิ่ม 46) · `lint:tokens` ไม่ regress ✓ · `npm run build` ✓

**พิสูจน์ว่าเทสต์จับได้จริง** — ทดลองทำพังสองแบบแล้วดูว่าแดงตรงจุด:

| ทดลองทำพัง | เทสต์ที่แดง |
|---|---|
| ให้ตัวหารมาจาก `extra_data` สด (ความผิดพลาดแบบ `FILL:`) | "stays within the question's worth after the teacher deletes a point" |
| ตัดจุดที่ไม่มีเฉลยออกจากเฉลยที่ตรึง | 3 เทสต์ใน `imageLabelKey` รวมข้อที่คุมสัญญาเรื่องตำแหน่ง |

restore กลับแล้วเขียวครบทั้งสองครั้ง

**ยังไม่ได้ตรวจ**: ไม่มีอะไรแตะ Supabase, server action หรือ browser ในเฟสนี้ — ไม่มีให้ตรวจ

### งานที่เฟส 1 ทิ้งไว้ให้เฟสหลัง

- `lib/exam-screen-qa-fixture.test.ts` ยืนยันว่า fixture ของห้องทดลองครอบคลุมทุกประเภท
  ตอนนี้ยังไม่มี `image_label` อยู่ในนั้น **เป็นงานของเฟส 4** พร้อมกับตัวเรนเดอร์
- `lib/actions/questions.ts` ยังไม่ต้องแก้ เพราะยังไม่มีฟอร์มไหนเขียน `ImageLabelConfig` ลงไป — เฟส 6

### เฟส 2 — ทำไมต้องแยก 2 ไฟล์

Postgres ใช้ค่า enum ใหม่ในทรานแซกชันเดียวกับที่ `ADD VALUE` ไม่ได้ และ CLI รันหนึ่งทรานแซกชันต่อไฟล์

- ไฟล์ 1: `ALTER TYPE question_type ADD VALUE IF NOT EXISTS 'image_label'` อย่างเดียว
- ไฟล์ 2: `CREATE OR REPLACE FUNCTION public.education_research_question_max_score` เพิ่มสาขาใหม่
  **ยกตัวฟังก์ชันเดิมมาทั้งดุ้นด้วยสคริปต์ ห้ามพิมพ์ใหม่** — พิมพ์ผิดตรงนี้จะเปลี่ยนคะแนนเต็ม
  ของทุกประเภทโดยไม่มีอะไรฟ้อง (ดู `20260912073314_classify_research_max_score.sql` เป็นแบบ)
  ถ้าไม่มีสาขานี้ โจทย์ติดป้ายจะตกไปทาง `answer_parts` แล้วรายงานคะแนนเต็ม = 1 เสมอ
  โครงการวิจัยจะได้ยอด pre/post ที่ผิดโดยไม่มีอะไรพัง

เทสต์ PGlite (`lib/image-label-sql.test.ts`) ต้องทำสามอย่างเหมือน `lib/classify-sql.test.ts`:
รัน migration ทั้งสองไฟล์จริง · ตรวจว่า SQL ให้เลขเดียวกับ `imageLabelMarkerCount` ·
**ยืนยันลำดับ enum จากฐานจริงว่าตรงกับ `QUESTION_TYPE_ORDER` ที่เขียนด้วยมือ**

สร้างไฟล์ด้วย `supabase migration new <name>` เท่านั้น ห้ามตั้งชื่อเอง

## สิ่งที่เฟส 2 ทำไปแล้ว

- `supabase/migrations/20260916230103_add_image_label_question_type.sql` — `ALTER TYPE question_type ADD VALUE IF NOT EXISTS 'image_label'` อย่างเดียว
- `supabase/migrations/20260916230106_image_label_research_max_score.sql` — `CREATE OR REPLACE` ของ
  `education_research_question_max_score` เพิ่มสาขา image_label
  **ยกตัวฟังก์ชันเดิมมาทั้งดุ้นด้วยสคริปต์ ไม่ได้พิมพ์ใหม่** — diff เทียบ body ของเดิม: ลบ 0 บรรทัด เพิ่ม 68 บรรทัดที่เดียว
  กติกาของประเภทอื่นเหมือนเดิมทุกไบต์
- `lib/image-label-sql.test.ts` (ใหม่) — 24 เทสต์บน **PGlite**
- `lib/question-sort.ts` — export `QUESTION_TYPE_ORDER` ออกมา
- `lib/classify-sql.test.ts` — เทียบลำดับ enum กับ `QUESTION_TYPE_ORDER` ตัวจริงแทนสำเนาที่พิมพ์ไว้ในเทสต์

เทสต์ PGlite เล่นลำดับจริงของ production ทั้งเส้น: ฟังก์ชันต้นฉบับ → migration classify 2 ไฟล์ →
migration image_label 2 ไฟล์ แยกทรานแซกชันกันตามที่ CLI รัน
ความผิดพลาดตอนยกตัวฟังก์ชันจึงโผล่ที่นี่

### กับดักที่เจอระหว่างทาง — เทสต์ที่ผ่านทั้งที่ SQL พัง

ทั้งสองฝั่ง**ตรึงคำตอบขั้นต่ำไว้ที่ 1** (`|| 1` ฝั่ง TS · `GREATEST(item_count, 1)` ฝั่ง SQL)
เคสที่มีจุดเดียวจึงได้ 1 เท่ากันเสมอ ไม่ว่า SQL จะตัดสินเรื่อง reachability ถูกหรือผิด

ชุดเทสต์รุ่นแรกเขียนแบบนั้นเกือบทุกเคส ทดลองทำ SQL พังสองแบบแล้ว **เทสต์ผ่านหมด 23 ข้อ**
แก้โดยใส่ `anchor` — จุดที่ keyed แน่นอนในทุกโหมด — ไว้ข้างจุดที่แต่ละเคสต้องการทดสอบจริง ๆ
ทุกเคสจึงอยู่เหนือพื้น ที่ซึ่ง 1 กับ 2 เป็นคนละเลข

**ข้อนี้กระทบ `lib/classify-sql.test.ts` ด้วย** — หลายเคสในนั้นมีช่องเดียวและถูกพื้นกลบเหมือนกัน
เป็นงานคนละก้อน ไม่ได้แก้ในเบรนช์นี้

### ตรวจแล้ว

`tsc` ✓ · `npm test` **1116 ✓ (เพิ่ม 24)** · `lint:tokens` ✓
`supabase db push --dry-run` ยืนยันว่าจะส่งขึ้นเฉพาะ 2 ไฟล์นี้ ไม่มีอย่างอื่นพ่วง

**พิสูจน์ว่าเทสต์จับได้จริง** — ทำ SQL พัง 3 แบบแล้วดูว่าแดงตรงจุด:

| ทดลองทำพัง | เทสต์ที่แดง |
|---|---|
| ตัดกฎ reachability ทิ้ง | 4 เคส — bank ไม่มีคำนั้น · dropdown ที่ตัวเลือกไม่มีเฉลย · drag ที่ไม่มี bank · bank ที่มีของไม่ใช่คำ |
| ให้ option list ที่ว่างแปลว่า "ไม่มีตัวเลือกให้เลือก" | 3 เคสของ dropdown ทั้งสองทางที่ fallback |
| อ่าน bank ด้วย `jsonb_array_elements_text` (เสียการเช็กชนิด) | เคส bank ที่มีของไม่ใช่คำ |

restore กลับแล้วเขียวครบทุกครั้ง

### apply บนฐานจริงแล้ว 17 กันยายน 2026

เจ้าของผลิตภัณฑ์อนุมัติ · `supabase db push` (dry run ก่อน ยืนยันว่าส่งขึ้นเฉพาะ 2 ไฟล์นี้)
หลัง push `supabase migration list` แสดง **117 ตัว local = remote ครบทุกตัว ไม่เหลือ local-only เลย**
นี่คือขั้นที่ `20260726120000_file_upload_question_type.sql` เคยข้ามไป และทำให้ "ส่งไฟล์งาน" พังบน production ตั้งแต่กรกฎาคม

**ห้าม revert migration สองตัวนี้** — Postgres ลบค่า enum ออกไม่ได้ ถ้าต้องถอยต้องสร้าง type ใหม่ทั้งตัว
ปลอดภัยที่จะอยู่บนฐานก่อนที่ UI จะมี: การเพิ่ม label ไม่ rewrite แถวไหน
และยังไม่มีทางเขียนค่านี้ลงไปจนกว่าฟอร์มของเฟส 6 จะขึ้น

**สิ่งที่ยังไม่ได้ตรวจบนฐานจริง**: ตัวฟังก์ชันสร้างสำเร็จ (ถ้า body พังจะ push ไม่ผ่าน)
แต่ plpgsql ตรวจแค่โครงตอน `CREATE` — คำสั่ง SQL ข้างในจะถูก plan ครั้งแรกที่ถูกเรียกจริง
สาขา image_label จึงยัง**ไม่เคยรันบน Postgres ของ Supabase** และจะยังไม่รันจนกว่าจะมีแถว image_label ในฐาน (เฟส 6)
ที่พิสูจน์ไปแล้วคือรันบน Postgres จริงผ่าน PGlite ด้วยไฟล์เดียวกันเป๊ะ 24 เคส

### เฟส 3 — จุดที่เฉลยหลุดง่ายที่สุด

`SafeImageLabelMarker` **ต้องไม่มีฟิลด์ `answers` ตั้งแต่โครงสร้าง** ไม่ใช่ลบทีหลัง
และระวังจุดที่ต่างจาก `classify`:

- โหมด `drag`: `bank` **ต้องส่งไป** เพราะมันคือสิ่งที่นักเรียนต้องเห็น — ไม่ใช่เฉลย
- โหมด `dropdown`: `options` ของแต่ละจุด **ต้องส่งไป** แต่ห้ามส่งว่าอันไหนถูก
- `label` ของ marker **ห้ามส่ง** — มันคือชื่อที่ครูตั้งไว้ใช้บนหน้าตรวจ และมักจะเป็นคำเฉลยเอง
- `case_sensitive` ไม่ต้องส่ง นักเรียนไม่ได้ใช้

ต้องพิสูจน์ว่าเทสต์จับได้จริง: ทดลองแก้ sanitizer ให้ปล่อย `answers` ผ่าน แล้วต้องเห็นเทสต์ fail
(เทสต์แบบ "ต้องไม่มี" ที่ไม่เคยเห็นมัน fail เชื่อไม่ได้ — บทเรียนจากเฟส 3 ของ `classify`)

### เฟส 4 กับ 5 — ทำไมต้องแยกกัน

ถ้ารวมกันจะกลายเป็นเฟสที่ไม่มีวันจบ เพราะทุกครั้งที่แก้มือถือจะพังเดสก์ท็อป
และตอนนั้นยังไม่มีฟอร์มให้สร้างข้อมูลทดสอบด้วยซ้ำ

**เลย์เอาต์มือถือเป็นคนละเลย์เอาต์ ไม่ใช่การไหลใหม่**: ต่ำกว่า `lg` รูปแสดงพร้อม**หมายเลข ①②③ บนจุด**
แล้วช่องตอบย้ายลงมาเป็นรายการใต้รูป · โหมดลากกลายเป็น "แตะชิป แล้วแตะแถว"
ซึ่งเป็นทางที่ `matching-drag-input.tsx` ออกแบบไว้ให้เป็นทางหลักอยู่แล้ว ไม่ใช่ทางสำรอง

`classify` เลือก `lg` ไม่ใช่ `md` เพราะวัดมาแล้วว่าที่ 768/812 แถบข้างของหน้าสอบเบียดจนอ่านไม่ได้ —
เริ่มจากสมมติฐานเดียวกันแล้ววัดซ้ำ อย่าลอกตัวเลขมาเฉย ๆ

**responsive เป็น definition of done** (กฎใหม่ commit `525aaad`, 12 กันยายน 2026):
รองรับ iPad และมือถือทั้งสองแนว · ห้าม horizontal scroll ระดับหน้า · **touch target ≥ 44×44 px** ·
ข้อความไทยยาวต้องตัดบรรทัดได้ · ตรวจโดยย่อ-ขยายต่อเนื่อง ไม่ใช่แค่ความกว้างของอุปกรณ์ที่มีชื่อ

### เฟส 6 — ทำไมฟอร์มมาทีหลังตัวเรนเดอร์

แบบเดียวกับ `classify`: ใช้ fixture ใน `sample-questions.ts` เป็นข้อมูลทดสอบของเฟส 4–5 ไปก่อน
ตัวเรนเดอร์จึงถูกพิสูจน์เสร็จแล้วตอนที่ฟอร์มขึ้น แยกออกได้ว่าบั๊กอยู่ฝั่งไหน

สองอย่างที่ฟอร์มต้องทำตามบทเรียนของ `classify`:

- **บันทึกไม่ได้ถ้ายังใส่เฉลยไม่ครบทุกจุด** — กติกาตรวจยอมให้จุดไม่มีเฉลยได้ (ไม่นับคะแนน)
  แต่ครูที่เผลอเว้นจะเสียคะแนนของข้อไปเงียบ ๆ ฟอร์มต้องปฏิเสธแทนที่จะปล่อยผ่าน
  พร้อมตัวนับสด "7 จุด = 7 คะแนน · ยังไม่ได้ใส่เฉลย 2 จุด"
- **ฟอร์มต้องกันข้อมูลเสียก่อนถึงฐาน** — ลบคำออกจาก `bank` แล้วเฉลยของจุดที่ชี้คำนั้นต้องถูกล้าง ·
  ลบจุดแล้วต้องไม่เหลือ `box` กำพร้า

### เฟส 7 — ชื่อของแต่ละจุดบนหน้าตรวจ

หนึ่งบรรทัดต่อหนึ่งจุด **ตั้งชื่อด้วย `marker.label` ที่ครูตั้งไว้** ไม่ใช่ "จุดที่ 3"
ถ้าครูไม่ได้ตั้ง ให้ถอยไปใช้ "จุดที่ 3" ซึ่งไม่พังและไม่โกหก (ต้องมีเทสต์คุม)
**จุดที่ครูไม่ได้ใส่เฉลย ไม่ขึ้นบรรทัดเลย** — มันถูกกันออกจากคะแนนไปแล้ว
การขึ้นบรรทัดบอกผลของมันคือการกุคำตัดสินที่ไม่มีอยู่จริง

ในตารางผลสอบ (`results-client.tsx`) สรุปเป็น "ตอบ 5/7 จุด" — ลิสต์ของคำอ่านไม่รู้เรื่องในช่องตาราง

### เฟส 8 — ห้ามแตะ `DOMAIN.md` / `FEATURE_STATUS.md` ก่อนถึงเฟสนี้

`AGENTS.md` ห้ามนำเสนอ UI ที่ยังไม่เสร็จเป็นความจริงของ production
`classify` จงใจรอถึงเฟสสุดท้าย ทำแบบเดียวกัน

## สถานะตอนเริ่ม (17 กันยายน 2026)

- เบรนช์ `feat/image-label-question-type` แตกจาก `master` ที่ `fe687a3` · working tree สะอาด
- `git fetch origin` แล้ว `master` ตรงกับ `origin/master` (0/0) ไม่มี stash ไม่มี untracked
- `supabase migration list --linked` — **115 migration local = remote ครบทุกตัว ไม่มี local-only**
  เป็นสถานะที่ปลอดภัยที่จะเพิ่ม migration ใหม่
- เฟส 0 คือ commit `16e41fa` (เอกสารนี้ไฟล์เดียว) · เฟส 1 คือ `1986ceb` · เฟส 2 คือ commit ถัดไปบนเบรนช์เดียวกัน

## ความเสี่ยงที่ต้องรู้ก่อนทำต่อ

1. **เพิ่มค่า enum แล้วถอยไม่ได้** — Postgres ลบค่า enum ออกไม่ได้ ถ้าต้องถอยต้องสร้าง type ใหม่ทั้งตัว
   ค่า `image_label` จะขึ้นฐาน production ตั้งแต่เฟส 2 ก่อนที่ UI จะมี (ปลอดภัย: การเพิ่ม label
   ไม่ rewrite แถวไหน และไม่มีทางเขียนค่านี้ลงไปจนกว่าฟอร์มของเฟส 6 จะขึ้น) แต่ **ห้าม revert migration**

2. **`QUESTION_TYPE_ORDER` ใน `lib/question-sort.ts` เขียนด้วยมือ**
   ไม่มีอะไรใน JavaScript อ่านลำดับ enum กลับมาได้ ลืมต่อท้ายแล้วการเรียงคลังโจทย์ตามประเภท
   จะเพี้ยนเงียบ ๆ — เทสต์ PGlite ของเฟส 2 คือสิ่งเดียวที่จับได้

3. **`matching-drag-input.tsx` กับ `matching-line-input.tsx` ซ้ำโค้ด pointer กันอยู่แล้ว**
   ตัวเรนเดอร์ของเฟส 4 จะเป็นสำเนาที่สาม · **อย่า refactor สามตัวรวมกันในงานนี้** —
   สองตัวนั้นอยู่บนเส้นทางข้อสอบจริงที่ใช้งานอยู่ การแตะมันพร้อมกับเพิ่มของใหม่คือเอาความเสี่ยง
   สองก้อนมามัดรวมกัน · เขียนตัวใหม่แยกก่อน แล้วค่อยเปิดงานสกัด `useChipDrag` เป็นงานของมันเอง
   **บันทึกไว้ตรงนี้ว่านี่คือหนี้ที่ตั้งใจก่อ ไม่ใช่ที่มองไม่เห็น**

4. **โหมดพิมพ์ใบงานไม่มีแล้ว** — `AssignmentMode` เหลือ `'online'` อย่างเดียว
   ทั้งที่โจทย์ต้นแบบของประเภทนี้มาจากใบงานกระดาษ ถ้าจะให้พิมพ์ได้เป็นงานคนละก้อนที่ต้องตัดสินใจแยก

## สิ่งที่ตั้งใจไม่ทำในรุ่นแรก — เพิ่มทีหลังได้โดยไม่ทำลายโจทย์เก่า

1. **ครูลากกล่องคำตอบเอง** — `box?` เป็น optional อยู่แล้ว
2. **โหมดต่อจุด** (จุดหนึ่งพิมพ์ จุดหนึ่งดรอปดาวน์) — ย้าย `answer_mode` ลงไประดับ marker
3. **สลับลำดับคลังคำ** — ต้องคิดเรื่องคอลัมน์เก็บลำดับใหม่ ห้ามยัดลง `option_order`
4. **จุดที่ครูตรวจเอง** — ต้องเปิดสถานะ `is_correct = null` ทั้งสาย
5. **เลือกพื้นที่ (hotspot) แทนจุด** — คนละโจทย์ ไม่ใช่ส่วนขยาย
