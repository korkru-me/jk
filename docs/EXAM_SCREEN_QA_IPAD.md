# รายการทดสอบหน้าข้อสอบบน iPad จริง — เฟส 3

อัปเดต: 21 กันยายน 2026 · **agent-only ผ่านแล้ว — physical-device UAT กำลังดำเนินการ**

ใช้เฉพาะหน้า `/exam-screen-lab` และข้อมูลจำลอง ห้ามใช้บัญชี คำตอบ รูป หรือไฟล์ของนักเรียนจริง การตรวจในเบราว์เซอร์จำลองไม่ใช่หลักฐานแทน Safari, SEB, นิ้ว, Apple Pencil หรือคีย์บอร์ด iPad จริง

## ผล agent-only

- ตรวจ 512×1024 (Split View), 768×1024 (iPad มาตรฐาน), 1024×1366 และ 1366×1024 ทั้งสามข้อต่อหน้า ไม่พบ document overflow หรือ control หลุดซ้าย/ขวา
- iPad แนวตั้งและ Split View ซ่อน navigator ด้านขวาที่เคยบีบเนื้อหา แล้วใช้ปุ่ม **ดูทุกข้อ** เปิด dialog ที่มี touch target 44px, เวลา, สถานะทุกข้อ และปุ่มส่งแทน; iPad แนวนอน/จอกว้างยังใช้ navigator ด้านขวา
- dialog ดูทุกข้อและยืนยันส่งรองรับความสูง viewport จริง, safe-area, scroll ภายใน, Escape และ focus trap
- แป้นคณิตศาสตร์และเครื่องคิดเลขจำกัดความสูงด้วย `--app-height`/`100dvh`, เลื่อนภายในได้ และไม่หลุดจอใน Split View
- กระดาษทดเต็มจอจนถึง breakpoint `lg`; toolbar ไม่ถูกบีบเป็นแผงข้างบน iPad แนวตั้ง และปุ่มหลักเพิ่มขนาดสำหรับ pointer แบบ coarse
- จับคู่แบบช่อง, โยงเส้น และเรียงลำดับผ่านการกดจำลอง; สำหรับโยงเส้น การ์ดที่ยังตอบได้ทั้งสองฝั่งเป็นพื้นที่ลากเต็มใบและไม่เปิดให้ browser แย่งเป็นการเลื่อนหน้า ส่วนการเลื่อนหน้ายังทำได้จากพื้นที่ว่างนอกการ์ด
- Next MCP รายงาน compilation/config/session issues ว่าง และ browser console ไม่มี runtime error
- axe WCAG A/AA พบเฉพาะ contrast ของลายน้ำโปร่งใสที่ตั้งใจเป็นสิ่งตกแต่งและถูก `aria-hidden`; ไม่พบ violation ของ control หรือเนื้อหาที่ผู้ใช้ต้องอ่าน
- `npm test` ผ่าน 96 files / 1,310 tests; TypeScript, design-token lint และ production build 63 static pages ผ่าน

## สิ่งที่ต้องทดสอบบน iPad จริงหลังเฟส 8

- จดรุ่น iPad, iPadOS, Safari/SEB และว่ามี Apple Pencil หรือไม่
- หมุนแนวตั้ง/แนวนอนและเข้า Split View ทั้งหนึ่งและสามข้อต่อหน้า ขณะเปิด/ปิดคีย์บอร์ดไทย
- เปิด **ดูทุกข้อ**, เวลา, โหมดโฟกัส และกล่องยืนยันส่ง ตรวจว่าปุ่มไม่ถูก safe-area หรือคีย์บอร์ดบัง
- จับคู่ทั้งสองแบบและเรียงลำดับด้วยนิ้ว; หากมี Pencil ให้ทำซ้ำและตรวจว่าการเลื่อนหน้าไม่ถูกการลากแย่ง
- เปิดเครื่องคิดเลข แป้นคณิตศาสตร์ และกระดาษทด วาด/ลบ/หมุนจอ แล้วตรวจว่างานไม่หาย
- ใช้ไฟล์สมมติทดสอบ camera/file picker ทั้งยกเลิก เลือก และลบไฟล์ โดยไม่ส่งข้อมูลจริง

ผล physical UAT วันที่ 21 กันยายน 2026: เมนู **ดูทุกข้อ** และจับคู่แบบช่องผ่าน
รอบ `r6` ของจับคู่แบบลากเส้นพบว่าการ์ดลากได้เพียงขวาไปซ้าย แต่ซ้ายไปขวาไม่เริ่ม
และการลากเฉียงจากฝั่งขวาบางครั้งกลายเป็นเลื่อนหน้า จึงทำให้การ์ดทั้งสองฝั่งเป็น
พื้นที่ลากเฉพาะ ใช้สถานะกดค้างแบบเดียวกัน และย้ายการเลื่อนหน้าไปพื้นที่ว่างนอก
การ์ด จากนั้น deploy และล็อก Staging candidate `r7` แล้ว รอทดสอบซ้ำบน iPad จริง

ผลทดสอบซ้ำบน iPad จริงของ candidate `r7`: **ผ่านทั้งหมด** — ลากจากพื้นที่
การ์ดได้ทั้งซ้าย→ขวาและขวา→ซ้าย การลากเฉียงไม่กลายเป็นเลื่อนหน้า และสถานะ
กดค้างของทั้งสองฝั่งสม่ำเสมอ จากนั้นการกดค้างแล้วลากเรียงลำดับด้วยนิ้วผ่าน:
การ์ดย้ายตำแหน่งสำเร็จและหน้าไม่เลื่อนแทน โหมดโฟกัสผ่านทั้งการเข้า/ออก
การเลื่อนโดยไม่มีเนื้อหาหรือปุ่มถูกบัง และลำดับเดิมยังอยู่ รายการถัดไปคือ
กล่องยืนยันส่งคำตอบ ซึ่งผ่านทั้งการแสดงผล ปุ่มไม่ถูกบัง การเลื่อนภายใน การยกเลิก
และข้อมูลเดิมไม่หาย รายการถัดไปคือเครื่องคิดเลขวิทยาศาสตร์

เครื่องคิดเลขวิทยาศาสตร์บน candidate `r7` ผ่านการคำนวณ `2 + 3 = 5`, การเลื่อน
เพื่อเข้าถึงปุ่มทั้งหมด, การหมุนแนวตั้ง/แนวนอนโดยแผงไม่หลุดจอ และการปิดโดย
คำตอบเดิมไม่หาย แป้นคณิตศาสตร์ของช่องคำตอบผ่านต่อจากนั้นทั้งปุ่ม การเลื่อนภายใน
และการคงค่าหลังปิดแผง

หลังผลดังกล่าว ผู้ใช้ขอให้เครื่องคิดเลขแสดง comma คั่นหลักพัน จึงเพิ่มการจัดรูปแบบ
เฉพาะหน้าจอ โดยสูตรดิบ ผลที่ใส่กลับคำตอบ และ comma ที่เป็นตัวคั่น argument ยัง
ทำงานตามเดิม Local และ canonical Staging ยืนยัน `1900052` → `1,900,052`,
`1,900,052 + 1,000` → `1,901,052`, การวาง `1,500+23,000` → `24,500`
รวมถึง Backspace ตรงตำแหน่ง comma แล้ว candidate ถูกล็อกใหม่เป็น `r8` รอให้
ผู้ใช้ยืนยันการแสดงผลนี้บน iPad จริง ผลคือ **ผ่าน** ขั้นถัดไปคือกระดาษทด แล้วจึง
ทดสอบ camera/file picker ต่อ

บันทึกเฉพาะวันที่ รุ่นอุปกรณ์/OS/แอป แนวจอ กรณีที่ทดสอบ และอาการที่ไม่มีข้อมูลส่วนบุคคล ห้ามส่งหรือ commit password, CK/BEK, token, คำตอบจริง รูปลายมือจริง หรือไฟล์ข้อสอบจริง
