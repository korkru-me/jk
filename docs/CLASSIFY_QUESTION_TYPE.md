# ประเภทโจทย์ `classify` (ตารางจำแนก)

อัปเดตล่าสุด: 12 กันยายน 2026 · สถานะ: **ครบทั้ง 7 เฟส — ใช้งานได้จริงแล้ว**
· เบรนช์ `feat/classify-question-type` · migration apply บนฐานจริงแล้ว

เอกสารนี้คือช่องทาง handoff ของงานนี้ตาม `AGENTS.md` แชตและ memory ของ agent ไม่ใช่ช่องทาง handoff
ใครมาทำต่อ (Claude Code หรือ Codex เครื่องไหนก็ตาม) ให้เริ่มจากไฟล์นี้

## โจทย์ที่ต้องการรองรับ

ใบงานรูปแบบ "ตารางจำแนกประเภท" ที่ครูวิทยาศาสตร์ใช้กันทั่วไป — หนึ่งตาราง หนึ่งข้อ:

| ภาพ | จำแนกตามแหล่งกำเนิด | จำแนกตามชนิดของมอนอเมอร์ |
|---|---|---|
| (รูป) เนื้อหมู | ☑ พอลิเมอร์ธรรมชาติ ☐ พอลิเมอร์สังเคราะห์ | ☐ โฮโมพอลิเมอร์ ☑ โคพอลิเมอร์ |
| (รูป) ยางรัดของ | ☐ พอลิเมอร์ธรรมชาติ ☑ พอลิเมอร์สังเคราะห์ | ☑ โฮโมพอลิเมอร์ ☐ โคพอลิเมอร์ |
| (รูป) เชือกป่าน | ☑ พอลิเมอร์ธรรมชาติ ☐ พอลิเมอร์สังเคราะห์ | ☑ โฮโมพอลิเมอร์ ☐ โคพอลิเมอร์ |

**แถว** = สิ่งที่ถูกจำแนก (รูป + ชื่อ) · **คอลัมน์** = มิติการจำแนก + ชุดตัวเลือกของมิตินั้น · **ช่อง** = เลือก 1 ตัวเลือก

### ทำไมไม่ใช้ `composite` ที่มีอยู่

ทำได้ แต่ไม่ควร: 3 แถว × 2 คอลัมน์ = 6 parts ชนิด `mcq` ครูต้องพิมพ์ตัวเลือกซ้ำ 6 ชุด
ผังออกมาเป็น ก) ข) ค) เรียงแนวตั้ง ไม่ใช่ตาราง และที่สำคัญกว่านั้น
**ตัวเลือกเป็นของคอลัมน์ ไม่ใช่ของช่อง** — ไม่มีประเภทไหนที่มีอยู่ที่แสดงความสัมพันธ์นี้ได้

และคำตอบของ classify มีพิกัด 2 มิติ (แถว × คอลัมน์) ถ้าแบนเป็นลิสต์เดียวตามแบบ `composite`
หน้าผลสอบกับหน้าตรวจจะเห็นข้อย่อย 6 ข้อเรียงกันโดยไม่รู้ว่าข้อไหนคือแถวไหนคอลัมน์ไหน
ครูจะไม่มีวันเห็นว่า "เด็กพลาดมิติแหล่งกำเนิดเป็นพิเศษ" ซึ่งเป็นข้อมูลที่มีค่าที่สุดของโจทย์แบบนี้

## การตัดสินใจที่เจ้าของผลิตภัณฑ์เคาะแล้ว (12 กันยายน 2026)

| เรื่อง | ที่เลือก |
|---|---|
| ชื่อ type | `classify` — ป้ายไทย "ตารางจำแนก" |
| prefix เฉลย | `CLS:` (ตรึงลง `submission_answers.correct_answer` เปลี่ยนทีหลังไม่ได้) |
| คะแนน | **ช่องละ 1 คะแนน ได้คะแนนบางส่วน** — กติกาเดียวกับ `COMP:` และ `FILL:` |
| ขอบเขตรุ่นแรก | **แต่ละช่องเลือกได้ 1 ค่าเท่านั้น** (radio) โหมดหลายค่าเพิ่มทีหลังได้โดยไม่ทำลายโจทย์เก่า |
| เบรนช์ | `feat/classify-question-type` แตกจาก `fix/composite-part-images` ซึ่งแตกจาก `origin/master` |

## รูปร่างข้อมูล

`lib/types.ts` — `ClassifyConfig` เก็บใน `questions.extra_data`

```ts
columns: [{ id, title, options: string[] }]   // ตัวเลือกอยู่ที่คอลัมน์ ครูพิมพ์ครั้งเดียว
rows:    [{ id, text, image_urls?, answers: Record<columnId, optionIndex> }]
row_label_style?: PartLabelStyle              // undefined = 'number' (3.1, 3.2, …)
```

`answers` คีย์ด้วย **column id ไม่ใช่ตำแหน่ง** — สลับหรือลบคอลัมน์แล้วเฉลยของทุกแถวจะไม่เลื่อนไปชี้หมวดผิด

คำตอบนักเรียนและเฉลยที่ตรึงไว้ใช้รูปเดียวกัน: กริด row-major ของ **ตำแหน่งตัวเลือก**
`CLS:[[0,1],[1,0],[0,0]]` — ตำแหน่งไม่ใช่ข้อความ ด้วยเหตุผลเดียวกับที่ปรนัยเก็บ `MCQ:<position>`
(ตัวเลือกสองอันมีข้อความเหมือนกันได้ เทียบข้อความแล้วจะให้คะแนนผิดตัว)

## กติกาที่ต้องไม่แก้โดยไม่อ่านก่อน

ทั้งหมดอยู่ที่ `lib/classify.ts` ที่เดียว เพราะ **เฉลยที่ตรึง · คะแนนเต็ม · จำนวนข้อย่อยบนการ์ด ต้องเป็นเลขเดียวกัน**

1. **ช่องที่ครูไม่ได้ใส่เฉลย ไม่นับเป็นคะแนนเต็ม และไม่ถูกตรวจ**
   เคยพลาดมาแล้วกับข้อย่อยเติมคำของ `composite` — `naturalMaxScore` นับข้อย่อยที่ตัวเรนเดอร์ไม่มีช่องให้กรอก
   นักเรียนเสียคะแนนให้ช่องที่ครูไม่เคยให้มา (ดู `docs/FEATURE_STATUS.md`)
   ที่นี่ `classifyCellCount` นับเฉพาะช่องที่ตอบได้และตรวจได้ ทั้งสองฝั่งจึงตรงกันโดยโครงสร้าง

2. **เฉลยที่ชี้เลยจำนวนตัวเลือกของคอลัมน์ ถือเป็นช่องที่ไม่ได้ใส่เฉลย**
   เกิดจากการนำเข้าไฟล์ หรือครูลดจำนวนตัวเลือกทีหลัง ปล่อยไว้จะกลายเป็นช่องที่ไม่มีใครตอบถูกได้

3. **ตัวหารตอนให้คะแนนมาจากเฉลยที่ตรึงไว้ ไม่ใช่ `extra_data` ปัจจุบัน**
   ต่างจากสาขา `COMP:`/`FILL:` ที่อ่าน `naturalMaxScore` สด ๆ — ถ้าครูลบคอลัมน์ทิ้งหลังนักเรียนตอบไปแล้ว
   ตัวหารสดจะหดลงขณะที่ `earned` ยังมาจากกริดเดิมที่กว้างกว่า แล้ว `scaleScore` จะจ่ายเกินคะแนนที่โจทย์มี
   (มีเทสต์คุมไว้: `stays within the question's worth after the teacher deletes a column`)

4. **classify ไม่มีสถานะ "รอครูตรวจ"** — `is_correct` ไม่เคยเป็น null

## แผน 7 เฟส และสถานะ

| เฟส | งาน | สถานะ |
|---|---|---|
| 0 | ตัดสินใจ · ตรวจ `supabase migration list` · แตกเบรนช์ | ✅ เสร็จ |
| 1 | types + คะแนน + เทสต์ (TypeScript ล้วน ไม่แตะ DB ไม่แตะ UI) | ✅ เสร็จ |
| 2 | migration **2 ไฟล์แยกกัน** — `ALTER TYPE … ADD VALUE 'classify'` และสาขาใหม่ใน `education_research_question_max_score` | ✅ เสร็จ · apply บนฐานจริงแล้ว |
| 3 | `lib/exam-safe.ts` — `SafeClassifyConfig` **ต้องตัด `rows[].answers` ทิ้งก่อนถึง browser** | ✅ เสร็จ |
| 4 | ตัวเรนเดอร์ฝั่งนักเรียน (`exam-client.tsx`, `question-preview-content.tsx`) + responsive | ✅ เสร็จ |
| 5 | ฟอร์มสร้าง/แก้ · การ์ดเลือกประเภท · ป้ายชื่อ · ทำสำเนา · นำเข้า-ส่งออก | ✅ เสร็จ |
| 6 | ปลายทางหลังสอบ — `answer-feedback.ts`, หน้าตรวจ, หน้าผลสอบ | ✅ เสร็จ |
| 7 | เอกสาร (`DOMAIN.md`, `FEATURE_STATUS.md`) + ตรวจครบ + ส่งมอบ | ✅ เสร็จ |

**เฟส 2 ต้องแยก 2 ไฟล์จริง ๆ**: Postgres ใช้ค่า enum ใหม่ใน transaction เดียวกับที่ `ADD VALUE` ไม่ได้

## สิ่งที่เฟส 1 ทำไปแล้ว

- `lib/classify.ts` (ใหม่) — รูปร่าง เฉลย และการนับช่อง ที่เดียว
- `lib/classify.test.ts` (ใหม่) — 15 เทสต์
- `lib/types.ts` — `'classify'` ใน `QuestionType`, `ClassifyColumn`/`ClassifyRow`/`ClassifyConfig`, ต่อเข้า union ของ `extra_data`
- `lib/assignment-attempt.ts` — `naturalMaxScore` · เฉลยที่ตรึง `CLS:` · สาขาการให้คะแนน
- `lib/question-parts.ts` — นับ "ช่องจำแนก"
- `lib/question-sort.ts` — ต่อท้าย `QUESTION_TYPE_ORDER` ให้ตรงลำดับ enum ใน Postgres
- `lib/assignment-attempt.test.ts`, `lib/question-parts.test.ts`, `lib/question-sort.test.ts` — เทสต์เพิ่ม

**tsc บังคับให้เติม 3 จุดก่อนกำหนด** เพราะเป็น `Record<QuestionType, …>` ที่ต้องครบทุกค่า:
`lib/question-duplicate.ts` (route map) · `lib/question-portable.ts` (union) ·
`app/(app)/questions/new/_data/sample-questions.ts` (ตัวอย่างพอลิเมอร์จริง ใช้เป็น fixture ของเฟส 4 ต่อได้)
พร้อม `classifyConfig?` ใน `QuestionPreviewProps` เพื่อให้ตัวอย่างถือข้อมูลได้ — **ยังไม่มีตัวเรนเดอร์อ่านมัน**

ตรวจแล้ว: `tsc` ✓ · `npm test` 778 ✓ · `lint:tokens` ไม่ regress ✓ · `npm run build` ✓
ยังไม่ได้ตรวจ: ไม่มีอะไรแตะ Supabase, server action หรือ browser ในเฟสนี้ — ไม่มีให้ตรวจ

## สิ่งที่เฟส 2 ทำไปแล้ว

- `supabase/migrations/20260912073312_add_classify_question_type.sql` — `ALTER TYPE question_type ADD VALUE IF NOT EXISTS 'classify'` อย่างเดียว
- `supabase/migrations/20260912073314_classify_research_max_score.sql` — `CREATE OR REPLACE` ของ
  `education_research_question_max_score` เพิ่มสาขา classify **ยกตัวฟังก์ชันเดิมมาทั้งดุ้นด้วยสคริปต์ ไม่ได้พิมพ์ใหม่**
  (พิมพ์ผิดตรงนี้จะเปลี่ยนคะแนนเต็มของทุกประเภทโดยไม่มีอะไรฟ้อง) — diff เทียบกับของเดิมมีแค่บรรทัด `CREATE OR REPLACE` กับสาขาใหม่
- `lib/classify-sql.test.ts` (ใหม่) — รัน migration ทั้งสองไฟล์จริงบน **PGlite** (Postgres ตัวจริงคอมไพล์เป็น wasm ในโปรเซส)
  แล้วตรวจว่า SQL ให้เลขเดียวกับ `classifyCellCount` ครบ 12 กรณี · ยืนยันว่าประเภทอื่นคะแนนไม่ขยับ ·
  และยืนยันลำดับ enum จากฐานจริงว่าตรงกับ `QUESTION_TYPE_ORDER` ที่ `lib/question-sort.ts` เขียนไว้ด้วยมือ
  (ไม่มีอะไรใน JavaScript อ่านลำดับ enum กลับมาได้ การเรียงตามประเภทโจทย์จึงเพี้ยนเงียบ ๆ ได้ถ้าสองฝั่งไม่ตรงกัน)

ตรวจแล้ว: `tsc` ✓ · `npm test` 792 ✓ (เพิ่ม 14) · migration ทั้งสองไฟล์ apply สำเร็จบน PGlite แยก transaction กัน

**apply บน Supabase จริงแล้ว 12 กันยายน 2026** ด้วย `supabase db push` (dry run ก่อน ยืนยันว่าส่งขึ้นเฉพาะ 2 ไฟล์นี้)
หลัง push `supabase migration list` ไม่เหลือ local-only เลย — local กับ remote ตรงกันครบทุกตัว
นี่คือขั้นที่ `20260726120000_file_upload_question_type.sql` เคยข้ามไป

## สิ่งที่เฟส 3 ทำไปแล้ว

- `lib/exam-safe.ts` — `SafeClassifyColumn`/`SafeClassifyRow`/`SafeClassifyConfig` + สาขา sanitizer
  **`SafeClassifyRow` ไม่มีฟิลด์ `answers` เลย** ไม่ใช่ลบทิ้งทีหลัง — ไม่มีที่ให้มันไปอยู่ตั้งแต่แรก
  ต่างจากปรนัยที่เฉลยเป็นแค่ธง `is_correct` ข้างตัวเลือก เฉลยทั้งชุดของ classify อยู่ในแถว
  ถ้าหลุดไปคือนักเรียนได้ใบงานที่เฉลยมาให้เสร็จแล้ว
- `lib/exam-safe.test.ts` — เทสต์ว่าไม่มีเฉลยหลุด + โครงสร้างพัง ๆ ไม่ทำให้ browser ได้ขยะ

**ห้าม shuffle แถว คอลัมน์ หรือตัวเลือกในสาขานี้** — คำตอบนักเรียนเป็นกริด row-major ที่เทียบกับเฉลยที่ตรึงจาก config เดียวกัน
สับลำดับเมื่อไหร่ทุกช่องเลื่อนหมด (ปรนัยสับได้เพราะ attempt เก็บ `option_order` ไว้แมปกลับ classify ไม่มีและไม่ต้องมี)

ตรวจแล้ว: `tsc` ✓ · `npm test` 794 ✓ (เพิ่ม 2) · `lint:tokens` ✓ · `build` ✓
**พิสูจน์ว่าเทสต์จับได้จริง**: ทดลองแก้ sanitizer ให้ปล่อย `answers` ผ่าน เทสต์ fail ตามคาด แล้ว restore กลับ
(เทสต์แบบ "ต้องไม่มี" ที่ไม่เคยเห็นมัน fail เชื่อไม่ได้)

## สิ่งที่เฟส 4 ทำไปแล้ว

- `components/exam/exam-client.tsx` — `ClassifyAnswerInput` หน้าสอบจริง
- `components/questions/question-preview-content.tsx` — ตัวเดียวกันบวกผลถูก/ผิดรายช่องสำหรับครูลองตอบ
- `app/exam-screen-lab/_lib/fixture.ts` + เทสต์ — เพิ่มเคส classify เข้าห้องทดลอง
- ยืม `/exam-screen-lab` มาจากเบรนช์ SEB (commit แยกต่างหาก) เพื่อใช้ตรวจ

### เลย์เอาต์: `<table>` เดียว สองหน้าตา

เหนือ `lg` เป็นตารางจริง · ต่ำกว่า `lg` ทุก element ของตารางกลับเป็น block (`block lg:table-cell`)
แต่ละแถวจึงกลายเป็นการ์ดที่ซ้อนมิติลงมา · หัวตารางซ่อนตัวเองแล้วแต่ละช่องพิมพ์ชื่อคอลัมน์ของตัวเองแทน
(กลุ่ม radio ที่ไม่มีป้ายกำกับคือกลุ่มที่ตอบไม่ได้)

**ทำไม `lg` ไม่ใช่ `md`** — วัดมาแล้วไม่ได้เดา: ที่ 768 และ 812 (มือถือแนวนอน) ตารางไม่ล้นก็จริง
แต่แถบข้างของหน้าสอบเบียดจนสามคอลัมน์เหลือที่น้อยมาก "พอลิเมอร์ธรรมชาติ" หักเป็น 3 บรรทัด
กฎ responsive บอกให้ตัด breakpoint ตรงที่เนื้อหาเริ่มอ่านไม่ได้ ไม่ใช่ตรงความกว้างของอุปกรณ์ที่มีชื่อ
และบอกตรง ๆ ว่ามือถือแนวนอนต้องไม่ได้เลย์เอาต์แบบ desktop ที่อัดแน่น

### ตรวจแล้ว — วัดจาก DOM จริงทุกขนาด

| ความกว้าง | เลย์เอาต์ | บรรทัดต่อตัวเลือก | touch target | scroll แนวนอน |
|---|---|---|---|---|
| 375 (มือถือแนวตั้ง) | การ์ด | 1 | 53px | ไม่มี |
| 768 (แท็บเล็ตแนวตั้ง) | การ์ด | 1 | 53px | ไม่มี |
| 812 (มือถือแนวนอน) | การ์ด | 1 | 53px | ไม่มี |
| 1024 (แท็บเล็ตแนวนอน) | ตาราง | 1 | 53px | ไม่มี |
| 1280 (เดสก์ท็อป) | ตาราง | 1 | 53px | ไม่มี |

touch target ≥ 44px ตามกฎ · ไม่มี element ไหนล้นขอบขวาเลยสักขนาด

**ตรวจครบวงจรบนหน้าสอบจริง**: คลิกเลือกครบ 6 ช่องแล้วกด "ตรวจคำตอบข้อนี้"
ได้ **6/6 คะแนน** — คำตอบที่ตัวเรนเดอร์สร้าง `[[0,1],[1,0],[0,0]]` ตรงกับเฉลย `CLS:[[0,1],[1,0],[0,0]]` พอดี
คือ `gradeAnswer` ของเฟส 1 ทำงานกับสิ่งที่ UI ส่งมาจริง ไม่ใช่แค่กับ fixture ในเทสต์

`tsc` ✓ · `npm test` 802 ✓ · `lint:tokens` ✓ · `build` ✓

**ยังไม่ได้ตรวจด้วยตา**: ตัวเรนเดอร์ฝั่งพรีวิว (`question-preview-content.tsx`) — หน้าที่เรียกมันต้อง login ทุกหน้า
โครงสร้าง markup เหมือนตัวหน้าสอบที่ตรวจแล้วทุกบรรทัดและผ่าน tsc แต่ยังไม่เคยเห็นด้วยตา
จะเห็นครั้งแรกตอนเฟส 5 ผ่านปุ่มดูตัวอย่างในฟอร์ม

## สิ่งที่เฟส 5 ทำไปแล้ว

- `components/questions/classify-form.tsx` (ใหม่) · `app/(app)/questions/new/classify/page.tsx` (ใหม่)
- การ์ด "ตารางจำแนก" ในหน้าเลือกประเภท · `TYPE_LABEL`/`TYPE_SHORT` · ไอคอนในแดชบอร์ด · routing หน้าแก้ไข
- `lib/actions/questions.ts` — `ClassifyConfig` เข้า union ของ `extra_data`

ฟอร์มเรียงตามลำดับที่โจทย์ชนิดนี้ต้องการจริง: **คอลัมน์ก่อน แล้วค่อยแถว**
เพราะตัวเลือกเป็นของคอลัมน์ ตารางเฉลยด้านล่างจึงยังไม่มีอะไรให้เลือกจนกว่าคอลัมน์จะมีตัวเลือก
พิมพ์ตัวเลือกครั้งเดียว ทุกแถวได้ทันที

**สองจุดที่ฟอร์มกันข้อมูลเสียก่อนถึงฐาน** (เพราะ `classifyCorrectGrid` จะเงียบ ๆ ลดคะแนนเต็มให้):
ลบคอลัมน์แล้วเฉลยของมิตินั้นถูกลบออกจากทุกแถวด้วย · ลดจำนวนตัวเลือกแล้วเฉลยที่ค้างเลยปลายลิสต์ถูกล้าง

**บันทึกไม่ได้ถ้ายังใส่เฉลยไม่ครบทุกช่อง** — กติกาตรวจยอมให้ช่องไม่มีเฉลยได้ (ไม่นับคะแนน ไม่ถูกตรวจ)
แต่ครูที่เผลอเว้นจะเสียคะแนนของข้อไปเงียบ ๆ ฟอร์มจึงปฏิเสธแทนที่จะปล่อยผ่าน
ตัวนับสดใต้ตาราง ("4 ช่อง = 4 คะแนน · ยังไม่ได้เลือกเฉลย 2 ช่อง") บอกว่าเหลืออีกกี่ช่อง

### ตรวจแล้วบนเบราว์เซอร์จริง (มี session อยู่แล้วใน Browser pane)

- กรอกฟอร์มจริงทั้งใบ: ตัวเลือกที่พิมพ์ในคอลัมน์ไหลไปทุกแถวทันที · ตัวนับเตือนหายพอดีตอนใส่เฉลยครบ
- **เปิด "ดูตัวอย่าง" แล้วเห็นตัวเรนเดอร์ฝั่งพรีวิวของเฟส 4 เป็นครั้งแรก** — ตอบผิด 1 ถูก 1
  ช่องที่ตอบผิดขึ้นแดง เฉลยขึ้นเขียว สรุป "ถูก 1/2 ช่อง — ได้ 1 คะแนน" ตรงตามกติกาคะแนนบางส่วน
- responsive ที่ 375 และ 812: ไม่มี element ล้นขอบขวา ไม่มีปุ่มหลุดจอ

**เจอบั๊กระหว่างตรวจและแก้แล้ว**: ที่ 375px ปุ่มลบแถวถูกดันออกนอกจอ 53px กดไม่ได้เลย
เพราะ `flex-1` ไม่ยอมหดต่ำกว่าความกว้างของ toolbar ใน rich text editor — เติม `min-w-0` แก้จบ
(ถ้าไม่ได้วัด จะไม่มีทางเห็น เพราะ `document.scrollWidth` เท่ากับ `innerWidth` ไม่มี scroll ระดับหน้าให้สังเกต)

**หมายเหตุ touch target**: ปุ่มที่ฟอร์มนี้เพิ่มเองและสำคัญต่อการกด (ลบแถว/ลบคอลัมน์/ลบตัวเลือก/เพิ่มแถว/เพิ่มคอลัมน์/radio เฉลย)
สูง 44–53px ตามกฎ · ส่วน "เพิ่มตัวเลือก" สูง 34px เพราะใช้ `Button size="sm"` ตามที่ทุกฟอร์มในโปรเจกต์ใช้
ปุ่มของ design system ทั้งระบบสูง 34–38px อยู่แล้ว — ถ้าจะยกให้ถึง 44 ต้องแก้ที่ primitive ไม่ใช่ที่ฟอร์มเดียว

`tsc` ✓ · `npm test` 802 ✓ · `lint:tokens` ✓ · `build` ✓

**ทดสอบวงเต็มบนฐานจริงแล้ว** (เจ้าของผลิตภัณฑ์อนุญาต 12 กันยายน 2026):
บันทึก → การ์ดในคลังขึ้น "ตารางจำแนก" และ "2 ช่องจำแนก" (`subQuestionCount` ของเฟส 1 กับแถวจริง) →
เปิดหน้าแก้ไขแล้วค่ากลับมาครบทุกคอลัมน์/ตัวเลือก/แถว/เฉลย → ทำสำเนาแล้วเด้งไป `/questions/new/classify`
พร้อม "(สำเนา)" และข้อมูลครบ · เหลือเฉพาะส่งออก/นำเข้าไฟล์ `.korkru.json` ที่ยังไม่ได้ลองด้วยมือ
(union ของ `question-portable.ts` ต่อไว้แล้วตั้งแต่เฟส 1)

## สิ่งที่เฟส 6 ทำไปแล้ว

- `lib/answer-feedback.ts` — สาขา `CLS:` · `app/(app)/submissions/[id]/page.tsx` — หน้าครูตรวจ ·
  `results-client.tsx` — ช่อง "คำตอบ" ในตารางผลสอบ (สรุปเป็น "ตอบ 4/6 ช่อง" เพราะกริดของตัวเลขอ่านไม่รู้เรื่องในตาราง)

**หนึ่งบรรทัดต่อหนึ่งช่อง ตั้งชื่อตามตำแหน่งจริง** — "เนื้อหมู · จำแนกตามแหล่งกำเนิด"
ไม่ใช่ "ช่อง 3" เพราะสิ่งเดียวที่โจทย์ชนิดนี้ให้เด็กสังเกตคือแถวไหนคู่กับมิติไหน
ถ้าแสดงเป็นลิสต์แบนก็เท่ากับทิ้งข้อมูลที่มีค่าที่สุดของประเภทนี้ไป

**ต่างจากสาขา `COMP:` ตรงที่อ่าน config ของโจทย์ด้วย ไม่ใช่เฉลยที่ตรึงอย่างเดียว** —
กริดของตำแหน่งตัวเลือกอ่านไม่ออกถ้าไม่แปลงกลับเป็นคำ · ถ้าโจทย์ถูกแก้หลังนักเรียนส่งจนหา config ไม่เจอ
จะถอยไปใช้ "แถวที่ 1 · มิติที่ 1" และ "ตัวเลือกที่ 1" แทน ไม่พังและไม่โกหก (มีเทสต์คุม)

**ช่องที่ครูไม่ได้ใส่เฉลย ไม่ขึ้นบรรทัดเลย** — มันถูกกันออกจากคะแนนไปแล้ว
การขึ้นบรรทัดบอกผลของมันคือการกุคำตัดสินที่ไม่มีอยู่จริง

ตรวจแล้ว: `tsc` ✓ · `npm test` 809 ✓ (เพิ่ม 7) · `lint:tokens` ✓ · `build` ✓
**เห็นของจริงบนหน้าสอบ**: ตอบ 4 จาก 6 ช่อง (เว้น 2 ช่อง) กดตรวจ → "ถูกบางส่วน 4/6 คะแนน"
พร้อมบรรทัดรายช่องอ่านรู้เรื่อง ช่องที่เว้นขึ้น "—" คู่กับเฉลย · **ไม่มีสตริง `CLS:` หลงเหลือบนหน้าเลย**

## สิ่งที่เฟส 7 ทำไปแล้ว

- `docs/DOMAIN.md` — `classify` เข้ารายการประเภทโจทย์
- `docs/FEATURE_STATUS.md` — รายการในหัวข้อคลังโจทย์ พร้อมสิ่งที่ตรวจแล้วและยังไม่ได้ตรวจ

จงใจไม่แตะสองไฟล์นี้จนถึงเฟสสุดท้าย เพราะ `AGENTS.md` ห้ามนำเสนอ UI ที่ยังไม่เสร็จเป็นความจริงของ production

## สิ่งที่ยังไม่ได้ทำ — งานถัดไปของใครก็ตามที่มาต่อ

1. **ส่งออก/นำเข้าไฟล์ `.korkru.json`** — union ของ `question-portable.ts` ต่อไว้แล้ว แต่ยังไม่เคยลองด้วยมือ
2. **โหมดพิมพ์ใบงาน** — `components/exam/print-worksheet.tsx` ไม่รองรับ `classify` (และไม่รองรับ `composite` มาแต่เดิม)
   ทั้งที่โจทย์ต้นแบบของประเภทนี้มาจากใบงานกระดาษ เป็นงานคนละก้อนที่ต้องตัดสินใจแยก
3. **โหมดเลือกได้หลายค่าต่อช่อง** — รุ่นแรกตั้งใจรองรับเลือก 1 ค่าเท่านั้น
   เพิ่มทีหลังได้โดยไม่ทำลายโจทย์เก่า (ตั้งค่าเป็นราย column แล้วขยายกติกาคะแนน)
4. **รูปประกอบต่อแถว** — โครงสร้างและตัวเรนเดอร์รองรับ `rows[].image_urls` ครบแล้วและฟอร์มอัปโหลดได้
   แต่ยังไม่เคยทดสอบด้วยรูปจริงตั้งแต่ต้นจนจบ

## ข้อจำกัดที่ยังค้างอยู่ ต้องรู้ก่อนทำเฟสต่อไป

1. **schema พร้อมแล้ว แต่ค่า enum `classify` อยู่บนฐาน production ก่อนที่ UI จะมี**
   ปลอดภัย: การเพิ่ม label ไม่ rewrite แถวไหน และยังไม่มีทางเขียนค่านี้ลงไปจนกว่าฟอร์มของเฟส 5 จะขึ้น
   แต่แปลว่า **ห้าม revert migration สองตัวนี้** — Postgres ลบค่า enum ออกไม่ได้ ถ้าต้องถอยต้องสร้าง type ใหม่ทั้งตัว

   งาน SEB เฟส 3 (`20260905072556_add_exam_seb_password_drafts`) ยังค้างไม่ apply เหมือนเดิมบนเบรนช์ของมัน
   แต่ไม่มีบน `master` จึงไม่พ่วงมากับ classify — นั่นคือเหตุผลที่เบรนช์นี้แตกจาก master
   เมื่อเบรนช์ SEB rebase มาทับ master ทีหลัง มันจะเห็น migration ของ classify เป็นของที่ apply แล้ว ไม่ชนกัน

2. **`/exam-screen-lab` ไม่มีบน `master`** อยู่บนเบรนช์ SEB เท่านั้น (commit `47dd7a2`)
   เฟส 4 วางแผนจะใช้หน้านี้พิสูจน์ตัวเรนเดอร์โดยไม่ต้อง login และไม่ต้องมีข้อมูลใน DB
   ต้อง cherry-pick commit ห้องทดลองมา (เป็น dev-only ปิดใน production อยู่แล้ว)
   หรือพิสูจน์ผ่านปุ่มดูตัวอย่างใน `/questions/new` แทน — ตัดสินตอนเริ่มเฟส 4

3. **กฎ responsive ใหม่ (commit `525aaad`, 12 กันยายน 2026)** ทำให้ responsive เป็น definition of done
   กระทบเฟส 4 เต็ม ๆ: รองรับ iPad และมือถือทั้งสองแนว · ห้าม horizontal scroll ระดับหน้า ·
   **touch target ≥ 44×44 px** ซึ่งเป็นข้อที่ตารางที่มี radio เรียงเป็นกริดชนง่ายที่สุด ·
   ข้อความไทยยาวต้องตัดบรรทัดได้ · ตรวจโดยย่อ-ขยายต่อเนื่องไม่ใช่แค่ความกว้างของอุปกรณ์ที่มีชื่อ

4. **`DOMAIN.md` และ `FEATURE_STATUS.md` ยังไม่เขียนถึง classify โดยตั้งใจ** — จะเขียนในเฟส 7
   ตอนที่มันใช้งานได้จริง `AGENTS.md` ห้ามนำเสนอ UI ที่ยังไม่เสร็จเป็นความจริงของ production

5. **โหมดพิมพ์ยังไม่อยู่ในขอบเขต** — `components/exam/print-worksheet.tsx` ไม่รองรับแม้แต่ `composite`
   เป็นงานคนละก้อน ต้องตัดสินใจแยก
