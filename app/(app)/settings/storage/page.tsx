import { StorageSettings } from '@/components/settings/storage-settings'

export const metadata = { title: 'พื้นที่จัดเก็บไฟล์ — KorKru' }

export default function StoragePage() {
  return <StorageSettings />
}
