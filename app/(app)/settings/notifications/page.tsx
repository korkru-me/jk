import { NotificationSettings } from '@/components/settings/notification-settings'

export const metadata = { title: 'การแจ้งเตือน — KorKru' }

export default function NotificationsPage() {
  return <NotificationSettings />
}
