import { ExamDefaultsSettings } from '@/components/settings/exam-defaults-settings'

export const metadata = { title: 'ตั้งค่าข้อสอบเริ่มต้น — KorKru' }

export default function ExamDefaultsPage() {
  return <ExamDefaultsSettings />
}
